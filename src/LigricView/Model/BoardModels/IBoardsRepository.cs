using BoardsCommon;
using BoardsCore.Notifications.Delegates;
using Common.EventArgs;
using System.Collections.Generic;
using System;
using System.Threading.Tasks;

namespace BoardsCore.Abstractions.BoardsAbstractions.Interfaces
{
    //public interface IBoardsRepository
    //{
    //    IBoardRepository CurrentBoard { get; }

    //    IReadOnlyDictionary<long, IBoardRepository> Boards { get; }

    //    event ElementChangedHandler<IBoardRepository> CurrentBoardChanged;

    //    event EventHandler<NotifyDictionaryChangedEventArgs<long, IBoardRepository>> BoardsChanged;

    //    // TODO : Need refactoring. Should be abstraction
    //    Task AddBoard(string name, string publicKey, string privatekey);

    //    Task RemoveBoard(long key);

    //    Task SetNewCurrentBoard(IBoardRepository board);
    //}
}
